<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: linear-gradient(to right, #00c6ff, #134d94);
            font-family: 'Inter', sans-serif;
        }
        .card {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            border-radius: 15px;
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-8">
        <h1 class="text-5xl font-['gilroy'] font-bold mb-8 text-center text-white">Notify</h1>
        <div class="text-center mb-8">
            <a href="/create" class="bg-gradient-to-r from-blue-500 to-teal-400 text-white px-6 py-3 rounded-full shadow-md hover:shadow-lg hover:from-blue-600 hover:to-teal-500">Create New Note</a>
        </div>
        <div class="grid gap-8 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
            <% notes.forEach(note => { %>
                <div class="card p-6 rounded-xl shadow-lg overflow-hidden transform hover:scale-105 transition-transform duration-300">
                    <div class="p-6">
                        <h2 class="text-xl font-semibold text-gray-800 mb-2"><%= note.title %></h2>
                        <p class="text-gray-600 mb-4"><%= note.content %></p>
                        <div class="flex justify-between items-center">
                            <a href="/edit/<%= note._id %>" class="text-blue-500 hover:text-blue-700">Edit</a>
                            <form action="/delete/<%= note._id %>" method="post" class="inline">
                                <button type="submit" class="text-red-500 hover:text-red-700">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>
